credential.txt
node_modules/
.turbo/
dist/
build/
.next/
*.log
.DS_Store
.env
.env.local
.env.*.local
coverage/
.vite/
*.tsbuildinfo
.pnpm-store/
.pnpm-debug.log*

# Local SQLite databases (contain sessions, encrypted tokens)
apps/api/data/
*.db
*.db-shm
*.db-wal

# Editor/backup files
*.bak

# Per-service secret files (FCM Admin SDK JSON, APNs .p8, App Store key, etc.)
apps/api/secrets/

# Personal scratch / drafts not part of the project
threads-cdp-draft.md

# Product/business docs and screenshots — kept on disk for local reference
# but never published. Removed from history via git-filter-repo.
docs/
