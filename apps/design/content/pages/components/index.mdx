---
title: Components
description: Reference implementations of NoteKit's primitives, built from tokens.
---

These are the reference implementations of NoteKit's UI primitives — every one is rendered
live from [tokens](/foundations/color), monochrome and quiet, and adapts to the light/dark
toggle above the same way the product does.

<Cards>
  <Card title="Button" href="/components/button" description="Primary, secondary, ghost, and destructive actions." />
  <Card title="Text field" href="/components/input" description="Single-line text input, with and without an icon." />
  <Card title="Badge" href="/components/badge" description="Small status and metadata labels." />
  <Card title="Checkbox" href="/components/checkbox" description="Binary selection in forms and lists." />
  <Card title="Switch" href="/components/switch" description="Immediate on/off settings." />
  <Card title="Dialog" href="/components/dialog" description="Modal surface for focused decisions." />
</Cards>

## Where components live

NoteKit follows hexagonal architecture — the UI is a driving adapter. The specimens here
document the visual language; each product surface owns its own implementation and converges
by consuming the same tokens, never by sharing component code.

| Surface | Package / path | Basis |
| --- | --- | --- |
| Product (web, desktop, mobile) | `@notekit/core` → `adapters/driving/components` | Bespoke React, monochrome `.nk` theme, Inter |
| Backoffice (admin) | `apps/backoffice/.../components/ui` | shadcn/ui (Radix primitives) + Tailwind v4 |
| Marketing | `apps/landing/components` | Bespoke, animation-heavy, DM Sans |

## Registry roadmap

The [theme is installable today](/installation) via the shadcn registry. Per-component
installables (`npx shadcn add …/button.json`) follow once these primitives are extracted into
publishable, dependency-light source. This section grows one real component at a time — it
never lists a component that isn't rendered here.
