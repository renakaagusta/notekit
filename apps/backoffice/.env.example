# NoteKit API base URL (no trailing slash). All backoffice auth + data calls
# go here. The API's CORS allowlist must include this backoffice's origin.
#   - dev:   http://localhost:3001
#   - prod:  https://api.notekit.online
VITE_API_URL=http://localhost:3001
